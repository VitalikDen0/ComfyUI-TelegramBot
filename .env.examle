# Токен Telegram-бота (обязательно)
BOT_TOKEN=your_bot_token_here

# URL ComfyUI (по умолчанию http://127.0.0.1:8000)
COMFYUI_HOST=http://127.0.0.1:8000
COMFYUI_WS=ws://127.0.0.1:8000/ws

# Директории данных и выходных файлов
DATA_DIR=data
OUTPUT_DIR=Output
COMFYUI_SHARED_OUTPUT_DIR=Output

# Имя файла персистентности (по умолчанию bot_state.pkl)
PERSISTENCE_FILE=bot_state.pkl

# Проверять и автозапускать ComfyUI (true/false). По умолчанию false — клиент сам ищет уже запущенный экземпляр.
CHECK_COMFY_RUNNING=false

# Уровень логирования (DEBUG/INFO/WARNING/ERROR)
LOG_LEVEL=INFO
# Если LOG_LEVEL не DEBUG, держать DEBUG первые N секунд для старта
LOG_BOOT_DEBUG_SECONDS=30

# URL Telegram WebApp (Mini App) визуализатора (опционально)
WEBAPP_URL=https://example.com

# Параметры API для Mini App (сессии workflow)
WEBAPP_API_HOST=0.0.0.0
WEBAPP_API_PORT=8081
WEBAPP_API_ENABLED=true
WEBAPP_SERVE_ENABLED=false
WEBAPP_SERVE_PATH=webapp/dist

# Команда перезапуска ComfyUI (опционально)
# COMFYUI_RESTART_CMD=systemctl restart comfyui

# Путь к каталогу встроенных workflow-шаблонов ComfyUI (опционально)
# COMFYUI_WORKFLOW_TEMPLATES_DIR=/path/to/ComfyUI/workflow_templates
